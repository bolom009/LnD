For play this game you need just press enter and look into the folder snapshots, have a fun :)

Setup:
- git clone git@github.com:bolom009/LnD.git
- cd to directory
- go mod tidy

How to run:
- go run main.go (run game with cells 25x25, snapshots will save into path ./snapshots/)
- go run main.go -w 10 -h 10 (recommended, run game with cells 10x10 and snapshots will save into path ./snapshots/)